import { styled } from "styled-components";
import { cards, align } from "../../components/styles";
export const Conteiner = styled.section`
  ${align};
  margin: 100px auto;

  .cards {
    ${cards}
  }
`;
